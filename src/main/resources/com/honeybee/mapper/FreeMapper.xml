<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.honeybee.mapper.FreeMapper">


	<select id="getList" resultType="com.honeybee.domain.FreeVO">
		<![CDATA[
			select
				FNO, ID, TITLE, TO_CHAR(CONTENT) as content, HIT, THUMB, REGDT, UPDDT
			from FREE
			where FNO > 0 and DELDT is null
		]]>
	</select>

	<select id="getMyList" resultType="com.honeybee.domain.FreeVO">
			select
				FNO, ID, TITLE, TO_CHAR(CONTENT) AS CONTENT, HIT, THUMB, REGDT, UPDDT, DELDT
			from FREE
			where id = #{id} and DELDT IS NULL
	</select>

	<!-- CREATE -->
	<insert id="insert">
		<selectKey keyProperty="fno" order="BEFORE"
			resultType="long">
			select seq_free.nextval from dual
		</selectKey>
		
		insert into FREE (fno, title, content)
		values (#{fno}, #{title}, #{content})
	</insert>

	<!-- READ -->
	<select id="read" resultType="com.honeybee.domain.FreeVO">
		select
		FNO, ID, TITLE, TO_CHAR(CONTENT) AS CONTENT, HIT, THUMB, REGDT, UPDDT,
		DELDT
		from FREE where fno = #{fno}
	</select>

	<!-- UPDATE -->
	<update id="update">
		update FREE
		set id = #{id},
			title = #{title},
			content = #{content},
			upddt = sysdate
		where fno = #{fno}
	</update>

	<!-- 조회수 업데이트 -->
	<update id="updateHit">
		update FREE
		set hit = #{hit}
		where fno = #{fno}
	</update>

	<!-- DELETE -->
	<delete id="delete">
		update FREE
		set deldt = sysdate
		where fno = #{fno}
	</delete>

	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item='type' collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							title like '%' || #{keyword} || '%'
						</when>
						<when test="type == 'C'.toString()">
							content like '%' || #{keyword} || '%'
						</when>
						<when test="type == 'W'.toString()">
							id like '%' || #{keyword} || '%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>

	<select id="getListWithPaging" resultType="com.honeybee.domain.FreeVO">
		<![CDATA[
			select
				fno, id, title, content, hit, thumb, regdt, upddt
			from (
				select /*+INDEX_DESC(FREE pk_free) */
					rownum rn, fno, id, title, content, hit, thumb, regdt, upddt
				from FREE
				where
		]]>

		<include refid="criteria"></include>

		<![CDATA[
				rownum <= #{pageNum} * #{amount} and DELDT is null
			)
			where rn > (#{pageNum} -1) * #{amount}
		]]>
	</select>

	<select id="getTotalCount" resultType="int">
		select count(*) from FREE
		where

		<include refid="criteria"></include>

		FNO > 0 and DELDT is null
	</select>

	<update id="mypostremove">  <!-- 김세훈 테스트 -->
		update FREE
		set deldt = sysdate
		where fno = #{fno}
	</update>

</mapper>
